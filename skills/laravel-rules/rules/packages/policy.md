# Package policy — when to reach for a package

> Status: stub — to be deepened (Pass 2). Per-file template with a
> **Decision** section (when to add a dependency vs build it).

## Scope

Cross-cutting policy: the criteria for adding a third-party package vs
building in-house. The *which* (approved list) lives in
[catalog.md](catalog.md).

## TODO (Pass 2)

- Decision tree: maintenance/health, scope fit, lock-in, security
  surface, "is this core domain?" (don't outsource the domain).
- When to wrap a package behind a contract/service vs use it directly.
- Review/approval expectation before introducing a new dependency.

## Checklist

- _(to be written)_
