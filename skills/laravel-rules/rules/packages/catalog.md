# Package catalog — which packages to use

> Status: stub — to be deepened (Pass 2). A reference catalog of approved
> packages per concern, with a one-line rationale each.

## Scope

The project's approved/preferred packages. Reach for a catalogued
package before evaluating alternatives; add new entries via
[policy.md](policy.md).

## TODO (Pass 2)

- Table: concern → approved package → why → notes.
- Seed known choices (e.g. spatie/laravel-data for data objects;
  saloonphp for HTTP integrations — see the `saloon-laravel-integration`
  skill).
- Note any explicitly disallowed packages and the reason.

## Checklist

- _(to be written)_
