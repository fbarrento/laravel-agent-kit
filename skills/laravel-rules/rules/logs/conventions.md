# Logs

> Status: stub — to be deepened (Pass 2). Per-file template.

## Scope

Structured logging: channels, context, levels, and what to log / what
never to log. Inject the logger via the `Log` contextual attribute (see
[../architecture/dependency-injection.md](../architecture/dependency-injection.md)).

## TODO (Pass 2)

- Structured/contextual logging conventions.
- Channel/level selection.
- Never log secrets/PII — canonical home is
  [../security/secrets.md](../security/secrets.md); link-stub here, do not restate.
- What is worth logging (domain events vs noise).

## Checklist

- _(to be written)_
