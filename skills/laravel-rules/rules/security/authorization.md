# Authorization

> Status: stub — to be deepened (Pass 2). Per-file template + a Decision
> section (where authz is enforced).

## Scope

Who may do what: policies, gates, and where authorization is enforced.
Cross-cutting security concern (see
[../architecture/placement.md](../architecture/placement.md)).

## TODO (Pass 2)

- Decision: enforce at the boundary (controller/form request/middleware)
  vs inside the action — and how the two relate.
- Policies/gates conventions; naming.
- Authorization failure vs domain invariant
  ([../architecture/invariants.md](../architecture/invariants.md)) — distinct.
- Testing authorization.

## Checklist

- _(to be written)_
